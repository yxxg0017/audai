# AI 视觉对话助手实现思路与 PR 顺序

## 0. 提交与协作规则

后续所有项目工作必须遵守以下规则：

1. 不直接向 `main` 推送功能代码。
2. 每个模块新建独立分支，提交后创建 PR。
3. 每个 PR 只做一件事，粒度尽量小，便于审查。
4. PR 描述必须包含：
   - 标题：一句话说明本 PR 新增或修改了什么。
   - 功能描述：说明该功能的作用与使用方式。
   - 实现思路：说明技术选型或核心实现逻辑。
   - 测试方式：说明如何验证该功能正常运行。
5. PR 创建后由用户检查并 merge，Codex 不直接 merge。
6. `main` 分支在每次 merge 后都必须保持可运行。
7. 第三方库、框架和复用代码必须在 README 或 PR 描述中说明来源与用途。

## 1. 项目定位

本项目实现一个浏览器端 AI 视觉对话助手：

- 用户打开网页后授权摄像头和麦克风。
- 用户可以自然说话提问。
- AI 能听到用户语音，并结合摄像头当前画面回答。
- AI 回答应支持流式输出，优先保证低延迟和可打断。
- 系统需要控制调用成本，避免持续上传完整视频流。

## 2. 推荐总体架构

采用 Next.js 全栈应用作为主框架：

- 前端负责摄像头预览、麦克风采集、WebRTC 连接、会话状态和交互 UI。
- Next.js API Route 负责保护服务端 API Key、创建 Realtime 临时会话、代理视觉分析请求。
- OpenAI Realtime API + WebRTC 负责低延迟语音对话、语音转写、语音输出和打断。
- Vercel AI SDK 负责普通文本流、多模态消息状态、视觉分析结果的流式 UI 展示。
- 摄像头视频不直接持续上传，改为浏览器本地抽帧、压缩后按需发送。

### 2.1 数据流

```text
Camera Track -> 本地 video 预览
             -> Canvas 抽帧
             -> 压缩图片
             -> /api/vision
             -> 多模态模型分析
             -> 前端流式展示或写入会话上下文

Microphone Track -> RTCPeerConnection
                 -> Realtime session
                 -> 模型语音理解
                 -> 模型语音回复
                 -> remote audio 播放

Realtime Data Channel -> 转写文本、响应状态、VAD 事件、打断事件
                      -> 前端会话状态和消息列表
```

### 2.2 为什么不直接上传视频流

持续上传完整视频流会带来三个问题：

- 成本高：视频帧密集，视觉模型调用频率不可控。
- 延迟高：大体积图像或视频流会影响实时语音体验。
- 审查困难：不利于解释端云协同和成本控制策略。

更合理的方案是“实时语音 + 低频视觉抽帧”：

- 语音链路用 WebRTC 保证实时性。
- 视觉链路按需抽帧，例如用户提问时、场景变化时、或每 2-5 秒低频更新一次。
- 缓存最近视觉摘要，避免重复分析静止画面。

## 3. 关键技术参考

### 3.1 Vercel AI SDK 官方示例

重点参考：

- `streamText` / `useChat` 的流式消息组织方式。
- UI message、loading、error、abort 等状态管理。
- 多模态消息中图片和文本输入的组织方式。

在本项目中的用途：

- 用于视觉分析结果、文本补充回答和历史消息展示。
- 不强行承担实时音频链路，实时音频交给 WebRTC。

### 3.2 nextjs-openai-webrtc 或类似 WebRTC 实时对话项目

重点参考：

- 浏览器端 `RTCPeerConnection` 初始化方式。
- 麦克风 audio track 的采集与挂载方式。
- remote audio 的播放方式。
- data channel 事件处理。
- 用户插话时的中断逻辑。
- 会话断开、重连、资源释放逻辑。

在本项目中的用途：

- 实现低延迟语音对话。
- 实现模型说话时用户插话打断。
- 处理会话状态：idle、connecting、listening、thinking、speaking、error。

### 3.3 OpenAI Realtime / WebRTC

重点参考：

- 服务端创建临时 Realtime 会话，避免前端暴露长期 API Key。
- 前端通过 WebRTC 发送麦克风音频。
- 通过 data channel 接收转写、响应状态、音频完成、错误等事件。
- 使用服务端 VAD 或客户端状态判断支持自然轮次。

## 4. 成本控制策略

设计文档中需要明确“想到的”和“实际采用的”成本控制技巧。建议实际采用以下策略：

1. 视觉低频抽帧：默认不连续上传视频，只在提问或定时低频发送图片。
2. 图片压缩：抽帧后缩放到 512-768px 宽，并使用 JPEG/WebP 压缩。
3. 场景缓存：维护最近一帧图片 hash 或最近视觉摘要，画面变化不明显时不重复请求。
4. 会话摘要：长对话定期压缩历史，避免上下文无限增长。
5. 静音跳过：没有检测到语音或用户未提问时，不触发视觉分析。
6. 用户手动模式：提供“立即分析画面”按钮，避免后台无感消耗。
7. 服务端限流：API Route 增加频率限制，避免误触发或循环调用。

## 5. 新的 PR 顺序

### PR 1：初始化应用骨架

- 分支：`feature/app-scaffold`
- 标题：`feat(app): scaffold nextjs application`
- 目标：
  - 创建 Next.js + TypeScript 项目结构。
  - 配置 lint、基础样式和运行脚本。
  - 增加 `.env.example`。
- 实现边界：
  - 不接入真实 AI。
  - 不实现摄像头或麦克风逻辑。
- 验证方式：
  - `npm install`
  - `npm run lint`
  - `npm run dev`
  - 浏览器可打开首页。

### PR 2：实现基础交互界面和状态模型

- 分支：`feature/session-ui`
- 标题：`feat(ui): add conversation workspace and session states`
- 目标：
  - 搭建主界面：视频预览区、对话区、控制栏、状态指示。
  - 定义会话状态：idle、connecting、listening、thinking、speaking、error。
  - 增加开始、停止、静音、分析画面等按钮占位。
- 实现边界：
  - 使用 mock 数据，不调用摄像头、麦克风或模型。
- 验证方式：
  - 页面状态切换正常。
  - 移动端和桌面端布局不重叠。

### PR 3：采集摄像头和麦克风

- 分支：`feature/media-capture`
- 标题：`feat(media): capture camera and microphone streams`
- 目标：
  - 使用 `getUserMedia` 请求摄像头和麦克风权限。
  - 显示本地视频预览。
  - 展示麦克风输入状态。
  - 停止会话时释放所有 media tracks。
- 实现边界：
  - 只做本地采集，不上传音视频。
- 验证方式：
  - 首次打开会触发权限请求。
  - 摄像头画面能显示。
  - 停止后浏览器摄像头/麦克风占用消失。

### PR 4：实现视觉抽帧和本地压缩

- 分支：`feature/frame-capture`
- 标题：`feat(vision): capture compressed camera frames`
- 目标：
  - 从 video 元素抽取当前帧到 canvas。
  - 缩放图片并压缩为 JPEG/WebP。
  - 提供手动“分析画面”触发入口。
  - 记录最近抽帧时间和图片大小。
- 实现边界：
  - 暂不调用模型，只在前端预览抽帧结果和元数据。
- 验证方式：
  - 点击按钮后能得到压缩图片。
  - 压缩后体积和尺寸可见。
  - 连续点击不会造成 UI 卡顿。

### PR 5：接入视觉分析 API

- 分支：`feature/vision-api`
- 标题：`feat(vision): analyze captured frames with multimodal model`
- 目标：
  - 新增 `/api/vision`。
  - 服务端读取图片和用户问题，调用多模态模型。
  - 前端展示视觉分析结果。
  - README 增加环境变量说明。
- 实现边界：
  - 视觉分析先走手动触发。
  - 不和实时语音会话深度耦合。
- 验证方式：
  - 设置 API Key 后，可对当前画面提问。
  - 未设置 API Key 时返回清晰错误。
  - README 中列明依赖和模型用途。

### PR 6：创建 Realtime 会话 API

- 分支：`feature/realtime-session`
- 标题：`feat(realtime): create ephemeral realtime sessions`
- 目标：
  - 新增 `/api/realtime/session`。
  - 服务端创建临时 Realtime session。
  - 前端不暴露长期 API Key。
  - 定义 Realtime 会话配置和系统提示词。
- 实现边界：
  - 只返回临时凭据，不建立前端 WebRTC 连接。
- 验证方式：
  - API 可返回临时 session 信息。
  - 无 API Key 时返回明确错误。
  - 不在浏览器网络请求中出现长期 API Key。

### PR 7：接入 WebRTC 实时语音

- 分支：`feature/webrtc-audio`
- 标题：`feat(realtime): stream microphone audio over webrtc`
- 目标：
  - 浏览器创建 `RTCPeerConnection`。
  - 将麦克风 audio track 加入 WebRTC。
  - 播放模型返回的 remote audio。
  - 使用 data channel 接收基础事件。
- 实现边界：
  - 先完成语音输入和语音输出闭环。
  - 暂不实现复杂打断和视觉上下文融合。
- 验证方式：
  - 用户说话后模型能语音回应。
  - 断开会话后音频连接释放。
  - 刷新页面不会残留连接。

### PR 8：实现转写、状态同步和打断机制

- 分支：`feature/realtime-turns`
- 标题：`feat(realtime): handle transcripts states and interruptions`
- 目标：
  - 展示用户语音转写和 AI 回复状态。
  - 根据 Realtime data channel 事件同步 listening、thinking、speaking 状态。
  - 用户插话时取消当前 AI 回复或停止播放。
  - 增加停止、重连、错误恢复逻辑。
- 实现边界：
  - 重点保证语音对话自然度。
  - 不加入新的视觉模型能力。
- 验证方式：
  - AI 说话时用户插话能中断当前回复。
  - 多轮对话状态准确。
  - 网络或权限错误有可读提示。

### PR 9：融合视觉上下文和语音问题

- 分支：`feature/vision-realtime-context`
- 标题：`feat(vision): combine visual context with realtime conversation`
- 目标：
  - 用户语音提问时，按需抽取当前画面。
  - 将视觉摘要注入当前对话上下文。
  - 支持“看一下当前画面再回答”的自然交互。
  - 缓存最近视觉摘要，避免重复请求。
- 实现边界：
  - 视觉分析仍以抽帧为主，不上传连续视频。
  - 优先保证回答准确性和成本可控。
- 验证方式：
  - 用户问“我面前有什么”时能结合画面回答。
  - 静止画面重复提问不会频繁重复调用视觉分析。
  - README 或设计文档说明成本控制策略。

### PR 10：完善成本控制与安全边界

- 分支：`feature/cost-controls`
- 标题：`feat(cost): add visual throttling and request guards`
- 目标：
  - 增加视觉请求节流。
  - 增加图片大小限制。
  - 增加服务端频率限制或基础防滥用保护。
  - 增加会话清理和错误兜底。
- 实现边界：
  - 不新增大功能，只提高稳定性和可控性。
- 验证方式：
  - 高频点击不会触发无限请求。
  - 超大图片会被拒绝或压缩。
  - 错误状态能恢复到可继续使用。

### PR 11：补全设计文档和 README

- 分支：`docs/final-documentation`
- 标题：`docs(project): document user stories setup and cost controls`
- 目标：
  - 更新 README 的安装、启动、环境变量和 demo 指引。
  - 完成 `docs/design.md`。
  - 明确计划用户故事和实际实现用户故事。
  - 明确想到的成本控制技巧和实际采用的技巧。
  - 列出第三方依赖和原创功能边界。
- 实现边界：
  - 文档 PR，不改核心功能。
- 验证方式：
  - 新环境按 README 可以启动。
  - 设计文档覆盖题目要求的两项额外内容。

### PR 12：最终验收修复

- 分支：`fix/final-polish`
- 标题：`fix(app): polish final demo readiness`
- 目标：
  - 修复 demo 前发现的 UI、权限、错误提示或兼容性问题。
  - 确认 `main` 分支可运行。
  - 准备 demo 视频说明。
- 实现边界：
  - 只做验收修复，不塞入新大功能。
- 验证方式：
  - 从干净环境安装并启动成功。
  - 完成一次摄像头 + 麦克风 + 视觉问答演示。
  - README、设计文档、demo 视频信息一致。

## 6. MVP 范围

如果时间不足，优先保证以下最小可演示闭环：

1. 摄像头预览。
2. 麦克风授权和基础语音输入。
3. 手动抽帧分析当前画面。
4. 文本或语音方式回答“画面里有什么”。
5. README 和设计文档完整说明成本控制。

可以后置的能力：

- 自动场景变化检测。
- 完整语音打断优化。
- 更复杂的对话记忆。
- 多设备音频选择。
- 本地降噪或高级音频处理。

## 7. 风险与应对

### 7.1 WebRTC 调试成本高

应对：

- 先完成 `/api/realtime/session`，再接前端连接。
- 每一步都在 UI 中显示连接状态。
- 保留文本视觉问答作为 fallback，避免 demo 全部依赖实时语音。

### 7.2 浏览器权限和设备兼容

应对：

- 明确显示权限错误。
- 停止会话时释放 media tracks。
- demo 前固定使用 Chrome，并确认 HTTPS 或 localhost 环境。

### 7.3 成本不可控

应对：

- 默认手动视觉分析。
- 抽帧节流和压缩。
- 服务端限制图片大小和请求频率。
- 文档中明确端云协同设计。

### 7.4 最终提交被视为突击提交

应对：

- 严格按上述 PR 顺序持续提交。
- 每个 PR 都写清楚描述、实现思路和测试方式。
- 避免最后一天一次性导入大量代码。

## 8. 当前下一步

下一步应创建 PR 1：`feature/app-scaffold`。

完成条件：

- Next.js + TypeScript 项目可启动。
- README 增加本地启动方式。
- PR 描述包含功能描述、实现思路和测试方式。
- 用户审查并 merge 后，再进入 PR 2。

## 9. 语音视觉对话重构规划：HTTP POST + SSE + VAD + 视觉 Tool

### 9.1 重构目标

当前语音链路已经能证明麦克风采集、后端 STT 和本地 TTS 可以工作，但固定时间分片转写不适合作为正式语音对话能力。后续实现应参考小智 AI 的语音状态机思路，但保留本项目约束：不使用 WebSocket，继续使用 HTTP POST 上传语音 turn，通过 SSE 返回 STT、LLM、TTS 和视觉 tool 事件。

目标闭环：

1. 前端持续监听麦克风。
2. Web Audio VAD 检测一句话开始和结束。
3. 一句话结束后，将该 turn 音频通过 HTTP POST 发给本地 Node 语音服务。
4. 本地 Node 服务通过 SSE 返回 `stt.final`、`tool.call`、`tool.result`、`llm.delta`、`tts.audio`、`tts.stop`、`done`。
5. 如果 AI 播放时用户插话，前端取消当前 SSE 和音频播放，开启新的 turn。
6. 视觉问题通过关键词映射触发视觉 tool，前端补传当前摄像头压缩帧，本地服务完成视觉分析并注入回答上下文。

### 9.2 语音状态机

对齐小智的主要状态：

- `idle`：语音未启动。
- `listening`：麦克风已打开，VAD 等待用户说话。
- `user_speaking`：VAD 检测到用户正在说话，前端正在录制当前 turn。
- `transcribing`：当前 turn 音频已提交，本地服务正在转写。
- `thinking`：LLM 正在流式生成回复。
- `speaking`：TTS 正在播放。
- `error`：当前语音会话异常，可停止后重试。

默认 VAD 参数：

- 音量阈值：`0.035 RMS`。
- 起始确认：连续 `150ms` 超阈值。
- 结束确认：连续 `700ms` 低于阈值。
- 最短有效语音：`500ms`。
- 最大单句：`12s`。
- TTS 插话确认：播放时连续 `220ms` 超阈值。

### 9.3 本地 Node 语音服务接口

新增本地 Node 服务，默认端口 `8766`。

前端请求：

```text
POST http://127.0.0.1:8766/voice/turn
Content-Type: multipart/form-data
Accept: text/event-stream
```

表单字段：

- `sessionId`：当前语音会话 ID。
- `turnId`：当前用户发言 ID。
- `audio`：用户该 turn 的 WebM/Opus 音频。
- `config`：模型、API、TTS 声音、本地服务配置。

SSE 事件：

- `stt.final`：返回用户转写文本。
- `tool.call`：请求前端调用视觉 tool。
- `tool.result`：返回视觉摘要。
- `llm.delta`：返回 AI 文本增量。
- `tts.start`：TTS 开始。
- `metric`：返回 STT、视觉等待、首 token、首音频和总耗时等阶段指标。
- `tts.audio`：返回 base64 音频片段。
- `tts.stop`：TTS 结束。
- `done`：当前 turn 完成。
- `error`：当前 turn 失败。

### 9.4 视觉 Tool 设计

视觉 tool 不再只是手动“分析画面”，而是由语音文本触发：

- `analyze_current_frame`：关键词包括“画面”“看到”“看见”“镜头”“摄像头”“面前”。
- `identify_object`：关键词包括“这是什么”“是什么东西”“手里”“桌上”。
- `read_text`：关键词包括“读一下”“文字”“上面写了什么”“牌子”。
- `describe_scene`：关键词包括“有什么”“描述”“环境”“周围”。
- `locate_object`：关键词包括“在哪里”“找一下”“有没有”。

触发流程：

1. 本地 Node 服务拿到 `stt.final` 文本后先做关键词匹配。
2. 命中视觉关键词时，SSE 返回 `tool.call`。
3. 前端收到 `tool.call` 后抓取当前摄像头压缩帧，POST 到 `/voice/tool-result`。
4. 本地 Node 服务收到图片后返回 `tool.result`，表示图片已进入本轮回答上下文。
5. LLM 回复时直接使用图片和用户问题做一次多模态流式回答，避免视觉摘要模型和文本模型串行调用。

### 9.5 PR 拆分

建议后续按以下 PR 推进：

1. `feat(voice): 引入 VAD turn 状态机`
   - 替换固定分片 STT。
   - 保留当前页面布局和设置入口。
   - 让连续两句话生成两个独立 turn。

2. `feat(local): 增加本地 Node 语音 SSE 服务`
   - 新增 `scripts/local_voice_server.mjs`。
   - 实现 `/voice/turn`、`/voice/tool-result`、`/health`。
   - 接入 whisper.cpp、chat completions、macOS `say`。

3. `feat(voice): 支持插话打断`
   - AI 播放时继续 VAD。
   - 检测用户说话后中断当前 SSE 和音频。
   - 自动开启新 turn。

4. `feat(vision): 将视觉分析接入 tool 调用`
   - 实现关键词映射。
   - 收到 `tool.call` 后自动抓帧回传。
   - UI 展示 tool call 和 tool result。

### 9.6 验收标准

- 点击“开始语音”后，第一句话和第二句话都能独立识别。
- 静音和短噪声不会触发请求。
- AI 回复期间插话会停止当前播放并开启新 turn。
- 说“看一下画面里有什么”会自动触发视觉抓帧和视觉分析。
- 非视觉问题不会上传图像。
- 本地 Node 服务断开时，前端显示可恢复错误。
- `npm run lint`、`npm run typecheck`、`npm run build` 均通过。

### 9.7 当前实现进度

本模块当前已经落地为一个可演示的本地语音视觉流水线：

1. 前端使用 Web Audio 计算 RMS 音量，并按 VAD 参数识别一句话的开始与结束。
2. 每个有效语音 turn 使用 `MediaRecorder` 生成 WebM/Opus 音频，通过 `POST /voice/turn` 发给本地 Node 服务。
3. 前端通过 SSE 读取 `stt.final`、`llm.delta`、`tts.audio`、`tool.call`、`tool.result` 等事件。
4. UI 会持续显示 RMS、录音时长、上传音频大小、turnId 和最近 STT 文本，用于证明麦克风采集与后端转写链路真实生效。
5. 本地 Node 服务的 `/health` 会检查 `ffmpeg`、`whisper-cli`、本地模型文件和 macOS `say` 是否可用。
6. 视觉问题命中关键词后，Node 服务发出 `tool.call`，前端自动抓取或复用最近摄像头压缩帧并回传，服务端把图片直接并入本轮多模态流式 LLM 回复。
7. SSE `metric` 事件会返回 `stt_final`、`vision_wait`、`llm_first_token`、`tts_first_audio` 和 `total`，前端实时转写区域会展示阶段耗时。
8. 本地 TTS 默认使用 macOS `say`，设置页可切换为 Piper 本地声线，并通过 `.onnx` 模型路径选择声音。
9. AI 播放时继续保留 VAD 检测，用户插话会取消当前 SSE 请求并停止本地音频播放，然后回到监听状态。

### 9.8 当前 PR 验证方式

启动本地语音服务：

```bash
npm run voice:local
```

检查健康状态：

```bash
curl http://127.0.0.1:8766/health
```

启动前端：

```bash
npm run dev
```

浏览器验证：

1. 使用 `http://localhost:3000` 打开页面，保存 API Key 和 Base URL。
2. 点击“开始”，确认摄像头画面和麦克风电平可见。
3. 点击“开始语音”，说第一句话，确认“实时转写”区域显示 RMS、录音时长、上传大小和 STT 文本。
4. 等 AI 回复后再说第二句话，确认生成新的 turn，且第二句话也能转写。
5. 说“画面里有什么”或“看一下我面前是什么”，确认自动触发视觉 tool、抓帧和结合画面的回答。
6. 观察“实时转写”底部是否显示 STT、视觉等待、首 token、首音频和总耗时。
7. 在设置中把本地 TTS 引擎切换为 Piper，填写本地 `.onnx` 声线模型路径，确认本地服务健康检查和语音播放正常。
8. AI 播放时直接插话，确认当前播放被停止，页面回到监听或新一轮录音状态。

### 9.9 本轮低延迟优化规划与落地

图片推理延迟的主要来源是旧链路需要先调用视觉模型得到摘要，再调用文本模型生成回答。当前实现改为一次多模态流式回答：视觉 tool 只负责让前端提供当前压缩帧，Node 服务随后把图片和问题一起交给视觉模型流式输出。

已采用的优化：

1. 默认抽帧从较高质量压缩改为更小宽度和质量，降低 base64 体积和上传时间。
2. 视觉 tool 会复用 15 秒内的最近压缩帧，连续追问不重复 canvas 压缩。
3. SSE 增加 `metric` 事件，让 demo 时能看到 STT、视觉等待、首 token、首音频和总耗时。
4. TTS 生成改为排队并与后续 LLM token 读取并行，减少文本流被单句语音合成阻塞的时间。
5. 本地 TTS 抽象为 `say` 与 `piper` 两种引擎，默认 `say` 兜底，Piper 用于更自然的离线本地声线。
